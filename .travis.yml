sudo: required
dist: precise
language: minimal

services:
  - docker

before_install:
- docker login --username $USER --password $PASS

script:
- docker build -t djbelyak/npe:latest .
- docker push djbelyak/npe:latest