sudo: required

services:
  - docker

before_install:
- docker login --username $USER --password $PASS

script:
- docker build --build-arg crt=$CRT key=$KEY -t djbelyak/npe:latest .
- docker push djbelyak/npe:latest